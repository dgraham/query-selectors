{
  "name": "query-selectors",
  "version": "0.0.1",
  "description": "Use querySelector with Option return types",
  "repository": "dgraham/query-selectors",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "flow": "flow check",
    "lint": "eslint *.js test/",
    "prebuild": "npm run clean && npm run flow && npm run lint",
    "build": "babel index.js -d dist && cp index.js dist/index.js.flow",
    "pretest": "npm run flow && npm run lint",
    "test": "mocha --require babel-register",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "dom",
    "optional",
    "option",
    "some",
    "none",
    "maybe",
    "queryselector"
  ],
  "author": "David Graham",
  "license": "MIT",
  "dependencies": {
    "option-type": "^0.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "babel-register": "^6.24.1",
    "eslint": "^4.4.1",
    "eslint-plugin-prettier": "^2.1.2",
    "flow-bin": "^0.52.0",
    "jsdom": "^11.1.0",
    "jsdom-global": "^3.0.2",
    "mocha": "^3.5.0",
    "prettier": "^1.5.3"
  },
  "files": [
    "index.js",
    "dist"
  ]
}
